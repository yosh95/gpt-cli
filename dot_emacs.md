# .emacs sample

```
(load "~/gpt/gpt.el")

(setq default-enable-multibyte-characters t)
(set-language-environment "Japanese")
(global-set-key (kbd "C-x j") 'toggle-input-method)

(global-set-key (kbd "C-x g") 'gpt)
```
